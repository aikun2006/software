# 2026-07-05 工作日志

## 整合全套指令：按钮左移 + 英文大字导航修复 + 中英文文字隔离

### 修改文件
- `software/src/pages/visitor/index.vue`

### 修复内容
1. **isEnMode 响应性修复（关键Bug）**：
   - 原代码 `isEnMode` 为 `ref(false)`，`checkEnMode()` 函数定义了但从未调用
   - 导致用户须知弹窗在英文模式下仍显示中文
   - 修复：改为 `computed(() => currentLang.value === 'en')`，自动响应语言切换
   - 移除无用的 `checkEnMode` 函数

2. **computed 属性非法赋值修复**：
   - `watch(currentLang,...)` 和 `onMounted` 中对 `infoCards.value` 和 `spots.value` 赋值
   - 这两个是 computed 属性（只读），赋值会静默失败
   - 修复：移除对 computed 的赋值，仅保留对 ref（`cardDetails`、`activeCardData`）的赋值

### 无需修改的文件
- `software/src/styles/global.scss`：样式已在前次对话中完成
  - `.top-toggle-btn-group { transform: translateX(-24px) !important; }`（全模式左移）
  - `body.en-mode.large-font-mode { ... }`（英文大字导航修复）
  - `body.en-mode { ... }`（英文文字间距）
  - `body.large-font-mode { ... }`（大字模式全局放大）

### 构建结果
- `npm run build:h5` 成功，无错误
