# 灵山导览系统 - 环境变量配置
# 复制此文件为 .env 并填写实际值

# 字段加密主密钥（Fernet 格式，32字节 base64 编码）
# 生成方法：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
LINGSHAN_DB_KEY=

# 手机号 HMAC 盲索引密钥（可选，默认与 DB_KEY 相同）
LINGSHAN_HMAC_KEY=

# 访问口令（默认 lingshan2026）
LINGSHAN_PASS=lingshan2026
